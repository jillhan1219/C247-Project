# @package _global_
module:
  _target_: emg2qwerty.lightning.TDSConvCTCModule
  in_features: 528  # freq * channels = (n_fft // 2 + 1) * 16
  mlp_features: [528]  # Upscaled from [384] for Split-and-Share
  block_channels: [24, 24, 48, 48]  # Upscaled from [24,24,24,24] for Split-and-Share
  kernel_width: 32  # Total temporal receptive field of 125 samples given 4 layers
  spec_norm_type: RollingTimeNorm
  share_hand_weights: true  # Share MLP and TDS conv weights between left/right hands
  use_transformer: false  # Disable transformer, use pure TDS Conv
  transformer_d_model: 256
  transformer_nhead: 8
  transformer_num_layers: 6
  transformer_dim_feedforward: 1024
  transformer_max_len: 5000
  transformer_chunk_size: 5000

datamodule:
  _target_: emg2qwerty.lightning.WindowedEMGDataModule
  window_length: 8000  # 4 sec windows for 2kHz EMG
  padding: [1800, 200]  # 900ms past context, 100ms future context
